# Array

Array é um espaço contínuo na memória (Memória física do computador), que pode ser interpretado como tendo vários elementos.

Em JS isso é diferente, o array é um objeto, permitindo mais flexibilidade. A forma que vemos é de um array, mas por baixo, é algo totalmente diferente que permite que não precise ser um espaço contínuo.